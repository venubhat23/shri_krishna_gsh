<div class="container-fluid px-2 px-md-4">
  <!-- Modern Header -->
  <div class="row mb-4">
    <div class="col-12">
      <div class="text-center py-4 bg-gradient-success rounded-4 text-white shadow-lg position-relative overflow-hidden">
        <div class="position-absolute top-0 start-0 w-100 h-100 opacity-10">
          <div class="d-flex justify-content-around align-items-center h-100">
            <i class="fab fa-whatsapp fa-4x"></i>
            <i class="fas fa-mobile-alt fa-3x"></i>
            <i class="fas fa-users fa-3x"></i>
            <i class="fas fa-paper-plane fa-3x"></i>
          </div>
        </div>
        <div class="position-relative">
          <h1 class="display-5 fw-bold mb-2">
            <i class="fab fa-whatsapp me-3 bounce-animation"></i>
            WhatsApp Message Center
          </h1>
          <p class="lead mb-0 opacity-90">Send professional messages to your customers instantly</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Success/Error Messages -->
  <div id="message-container" class="mb-4" style="display: none;">
    <div id="message-alert" class="alert alert-dismissible fade show shadow-sm" role="alert">
      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
  </div>

  <!-- Two Main Sections -->
  <div class="row g-4 mb-5">

    <!-- Section 1: Individual & Business Messaging -->
    <div class="col-xl-6 col-lg-12">
      <div class="card border-0 shadow-lg h-100 modern-card">
        <div class="card-header bg-gradient-primary text-white border-0 py-4">
          <div class="d-flex align-items-center">
            <div class="bg-white bg-opacity-20 rounded-circle p-3 me-3">
              <i class="fas fa-user-friends fa-2x"></i>
            </div>
            <div>
              <h3 class="mb-1 fw-bold">Individual & Business</h3>
              <p class="mb-0 opacity-75">Send targeted messages to specific customers or manage business communications</p>
            </div>
          </div>
        </div>
        <div class="card-body p-4">

          <!-- Individual Customer Section -->
          <div class="mb-5">
            <div class="d-flex align-items-center mb-3">
              <div class="bg-primary bg-opacity-10 rounded-circle p-2 me-3">
                <i class="fas fa-user text-primary"></i>
              </div>
              <h5 class="mb-0 text-primary">Send to Individual Customer</h5>
            </div>

            <form id="individual-message-form">
              <div class="mb-3">
                <label for="customer-search" class="form-label fw-semibold">
                  <i class="fas fa-search me-2 text-muted"></i>Search Customer
                </label>
                <select id="customer-search" class="form-select form-select-lg modern-select" style="width: 100%;">
                  <option value="">Type customer name or phone number...</option>
                </select>
              </div>

              <div class="mb-4">
                <label for="individual-message" class="form-label fw-semibold">
                  <i class="fas fa-comment-alt me-2 text-muted"></i>Your Message
                </label>
                <textarea id="individual-message" class="form-control modern-textarea" rows="4" placeholder="👋 Hello! Thank you for being a valued customer of <%= AdminSetting.current.business_name %>!

🥛 We hope you're enjoying our fresh, pure desi cow milk.

📞 For any queries, feel free to contact us:
📱 WhatsApp: +91 9972808044
☎️ Call: +91 9008860329

🙏 Thank you for your continued trust!

🏠 <%= AdminSetting.current.business_name %>, Bangalore"></textarea>
              </div>

              <button type="submit" class="btn btn-primary btn-lg w-100 modern-btn" id="send-individual-btn">
                <span class="btn-content">
                  <i class="fab fa-whatsapp me-2"></i>
                  Send Message
                </span>
                <span class="btn-loader d-none">
                  <div class="spinner-border spinner-border-sm me-2" role="status">
                    <span class="visually-hidden">Loading...</span>
                  </div>
                  <span class="loading-text">Sending...</span>
                  <span class="loading-dots">
                    <span class="dot1">.</span>
                    <span class="dot2">.</span>
                    <span class="dot3">.</span>
                  </span>
                </span>
              </button>
            </form>
          </div>

          <!-- Unpaid Customers Section -->
          <div class="border-top pt-4">
            <div class="d-flex align-items-center mb-3">
              <div class="bg-warning bg-opacity-10 rounded-circle p-2 me-3">
                <i class="fas fa-exclamation-triangle text-warning"></i>
              </div>
              <h5 class="mb-0 text-warning">Payment Reminders</h5>
            </div>

            <form id="unpaid-message-form">
              <div class="row mb-3">
                <div class="col-6">
                  <label for="unpaid-month" class="form-label fw-semibold">Month</label>
                  <select id="unpaid-month" class="form-select modern-select">
                    <% Date::MONTHNAMES.compact.each_with_index do |month, index| %>
                      <option value="<%= index + 1 %>" <%= 'selected' if index + 1 == Date.current.month %>><%= month %></option>
                    <% end %>
                  </select>
                </div>
                <div class="col-6">
                  <label for="unpaid-year" class="form-label fw-semibold">Year</label>
                  <select id="unpaid-year" class="form-select modern-select">
                    <% (Date.current.year - 2..Date.current.year + 1).each do |year| %>
                      <option value="<%= year %>" <%= 'selected' if year == Date.current.year %>><%= year %></option>
                    <% end %>
                  </select>
                </div>
              </div>

              <div class="text-center mb-4">
                <button type="button" class="btn btn-warning btn-lg px-5 modern-btn" id="check-unpaid-btn">
                  <span class="btn-content">
                    <i class="fas fa-search me-2"></i>
                    Check Unpaid Customers
                    <small class="d-block mt-1 opacity-75">View payment reminders for this month</small>
                  </span>
                  <span class="btn-loader d-none">
                    <div class="spinner-border spinner-border-sm me-2" role="status">
                      <span class="visually-hidden">Loading...</span>
                    </div>
                    <span class="loading-text">Checking...</span>
                    <span class="loading-dots">
                      <span class="dot1">.</span>
                      <span class="dot2">.</span>
                      <span class="dot3">.</span>
                    </span>
                  </span>
                </button>
              </div>

              <div class="mb-4">
                <label for="unpaid-message" class="form-label fw-semibold">
                  <i class="fas fa-bell me-2 text-muted"></i>Payment Reminder Message
                </label>
                <textarea id="unpaid-message" class="form-control modern-textarea" rows="4" placeholder="🔔 Dear Valued Customer,

We hope you are enjoying our fresh desi cow milk! 🥛

💰 Your invoice is still pending. Please make the payment at the earliest to avoid any service interruptions.

📱 For payment queries: +91 9972808044
☎️ Call us: +91 9008860329

🙏 Thank you for your understanding and continued support!

🏠 <%= AdminSetting.current.business_name %>, Bangalore"></textarea>
              </div>

              <button type="submit" class="btn btn-warning btn-lg w-100 modern-btn" id="send-unpaid-btn" disabled>
                <span class="btn-content">
                  <i class="fab fa-whatsapp me-2"></i>
                  Send Payment Reminders
                </span>
                <span class="btn-loader d-none">
                  <div class="spinner-border spinner-border-sm me-2" role="status">
                    <span class="visually-hidden">Loading...</span>
                  </div>
                  <span class="loading-text">Sending...</span>
                  <span class="loading-dots">
                    <span class="dot1">.</span>
                    <span class="dot2">.</span>
                    <span class="dot3">.</span>
                  </span>
                </span>
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>

    <!-- Section 2: Bulk & Promotional Messaging -->
    <div class="col-xl-6 col-lg-12">
      <div class="card border-0 shadow-lg h-100 modern-card">
        <div class="card-header bg-gradient-success text-white border-0 py-4">
          <div class="d-flex align-items-center">
            <div class="bg-white bg-opacity-20 rounded-circle p-3 me-3">
              <i class="fas fa-bullhorn fa-2x"></i>
            </div>
            <div>
              <h3 class="mb-1 fw-bold">Bulk & Promotional</h3>
              <p class="mb-0 opacity-75">Send festive wishes and promotional messages to all customers</p>
            </div>
          </div>
        </div>
        <div class="card-body p-4">

          <form id="wishes-message-form">
            <div class="text-center mb-4">
              <div class="d-flex align-items-center justify-content-center mb-3">
                <div class="bg-success bg-opacity-10 rounded-circle p-3 me-3">
                  <i class="fas fa-users text-success fa-2x"></i>
                </div>
                <div>
                  <h5 class="mb-0 text-success">Send to All Customers</h5>
                  <small class="text-muted">Reach your entire customer base instantly</small>
                </div>
              </div>

              <button type="button" class="btn btn-outline-success btn-lg px-5 modern-btn" id="check-total-customers-btn">
                <span class="btn-content">
                  <i class="fas fa-users me-2"></i>
                  Check Total Customers
                  <small class="d-block mt-1 opacity-75">See how many customers will receive this message</small>
                </span>
                <span class="btn-loader d-none">
                  <div class="spinner-border spinner-border-sm me-2" role="status">
                    <span class="visually-hidden">Loading...</span>
                  </div>
                  <span class="loading-text">Counting...</span>
                  <span class="loading-dots">
                    <span class="dot1">.</span>
                    <span class="dot2">.</span>
                    <span class="dot3">.</span>
                  </span>
                </span>
              </button>
            </div>

            <div id="total-customers-info" class="mb-4" style="display: none;">
              <div class="alert alert-success text-center modern-alert">
                <div class="mb-2">
                  <i class="fas fa-users text-success fa-3x mb-3 pulse-animation"></i>
                  <h5 class="mb-1">Ready to Send! 📢</h5>
                </div>
                <div class="d-flex justify-content-center align-items-center">
                  <span id="total-customers-count" class="badge bg-success fs-3 me-2 px-4 py-2">0</span>
                  <span class="text-muted">active customers will receive your message</span>
                </div>
              </div>
            </div>

            <div class="mb-4">
              <label for="wishes-message" class="form-label fw-semibold">
                <i class="fas fa-heart me-2 text-muted"></i>Festive/Promotional Message
              </label>
              <textarea id="wishes-message" class="form-control modern-textarea" rows="7" placeholder="🎉 Happy Sankranti!

✨ May this auspicious festival bring joy, prosperity, and good health to you and your family!

🥛 Thank you for being a valued customer and trusting <%= AdminSetting.current.business_name %> for your daily milk needs.

🙏 We wish you a year filled with happiness and success!

📱 WhatsApp: +91 9972808044
☎️ Call: +91 9008860329

🏠 <%= AdminSetting.current.business_name %>, Bangalore
💚 Spreading joy with pure milk!"></textarea>
            </div>

            <div class="alert alert-info modern-alert mb-4">
              <div class="d-flex align-items-center">
                <i class="fas fa-info-circle fa-2x me-3"></i>
                <div>
                  <strong>Bulk Message</strong><br>
                  <small>This message will be sent to ALL active customers with valid phone numbers.</small>
                </div>
              </div>
            </div>

            <button type="submit" class="btn btn-success btn-lg w-100 modern-btn" id="send-wishes-btn">
              <span class="btn-content">
                <i class="fab fa-whatsapp me-2"></i>
                Send to All Customers
              </span>
              <span class="btn-loader d-none">
                <div class="spinner-border spinner-border-sm me-2" role="status">
                  <span class="visually-hidden">Loading...</span>
                </div>
                <span class="loading-text">Sending...</span>
                <span class="loading-dots">
                  <span class="dot1">.</span>
                  <span class="dot2">.</span>
                  <span class="dot3">.</span>
                </span>
              </span>
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

</div>

<!-- Total Customers Modal -->
<div class="modal fade" id="totalCustomersModal" tabindex="-1" aria-labelledby="totalCustomersModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg modal-dialog-scrollable">
    <div class="modal-content modern-modal">
      <div class="modal-header bg-gradient-success text-white border-0">
        <div class="d-flex align-items-center">
          <div class="bg-white bg-opacity-20 rounded-circle p-2 me-3">
            <i class="fas fa-users fa-lg"></i>
          </div>
          <div>
            <h5 class="modal-title mb-0 fw-bold" id="totalCustomersModalLabel">
              All Active Customers
            </h5>
            <small class="opacity-75">Ready to receive your message</small>
          </div>
        </div>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="d-flex justify-content-between align-items-center mb-3">
          <div class="d-flex align-items-center">
            <span class="badge bg-success fs-6 me-2" id="modal-total-count">0</span>
            <span class="text-muted">active customers with valid phone numbers</span>
          </div>
        </div>

        <div id="modal-total-customers-list" class="list-group">
          <!-- Customers will be loaded here -->
        </div>

        <div id="modal-total-loading" class="text-center py-4" style="display: none;">
          <div class="spinner-border text-success" role="status">
            <span class="visually-hidden">Loading...</span>
          </div>
          <div class="mt-2">Loading customer details...</div>
        </div>

        <div id="modal-no-total-customers" class="text-center py-4" style="display: none;">
          <i class="fas fa-users text-muted fa-3x mb-3"></i>
          <h5 class="text-muted">No Active Customers</h5>
          <p class="text-muted">No customers found with valid phone numbers.</p>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
          <i class="fas fa-times me-1"></i>
          Close
        </button>
        <button type="button" class="btn btn-success" id="modal-send-wishes-btn">
          <i class="fab fa-whatsapp me-1"></i>
          Send Message to All
        </button>
      </div>
    </div>
  </div>
</div>

<!-- Unpaid Customers Modal -->
<div class="modal fade" id="unpaidCustomersModal" tabindex="-1" aria-labelledby="unpaidCustomersModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg modal-dialog-scrollable">
    <div class="modal-content modern-modal">
      <div class="modal-header bg-gradient-warning text-dark border-0">
        <div class="d-flex align-items-center">
          <div class="bg-white bg-opacity-20 rounded-circle p-2 me-3">
            <i class="fas fa-exclamation-triangle fa-lg"></i>
          </div>
          <div>
            <h5 class="modal-title mb-0 fw-bold" id="unpaidCustomersModalLabel">
              Payment Reminders - <span id="modal-month-year"></span>
            </h5>
            <small class="opacity-75">Manage unpaid customer invoices</small>
          </div>
        </div>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="d-flex justify-content-between align-items-center mb-3">
          <div class="d-flex align-items-center">
            <span class="badge bg-warning text-dark fs-6 me-2" id="modal-unpaid-count">0</span>
            <span class="text-muted">customers with pending payments</span>
          </div>
          <div class="text-end">
            <small class="text-muted">Total Amount: </small>
            <span class="fw-bold text-danger" id="modal-total-amount">₹0</span>
          </div>
        </div>

        <div id="modal-customers-list" class="list-group">
          <!-- Customers will be loaded here -->
        </div>

        <div id="modal-loading" class="text-center py-4" style="display: none;">
          <div class="spinner-border text-warning" role="status">
            <span class="visually-hidden">Loading...</span>
          </div>
          <div class="mt-2">Loading customer details...</div>
        </div>

        <div id="modal-no-customers" class="text-center py-4" style="display: none;">
          <i class="fas fa-check-circle text-success fa-3x mb-3"></i>
          <h5 class="text-success">All Caught Up! 🎉</h5>
          <p class="text-muted">No unpaid customers found for this month.</p>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
          <i class="fas fa-times me-1"></i>
          Close
        </button>
        <button type="button" class="btn btn-warning" id="modal-send-reminder-btn" style="display: none;">
          <i class="fab fa-whatsapp me-1"></i>
          Send Payment Reminders
        </button>
      </div>
    </div>
  </div>
</div>

<!-- Loading Overlay -->
<div id="loading-overlay" class="d-none position-fixed top-0 start-0 w-100 h-100 d-flex justify-content-center align-items-center" style="background: rgba(0,0,0,0.5); z-index: 9999;">
  <div class="text-center text-white">
    <div class="spinner-border text-light mb-3" role="status">
      <span class="visually-hidden">Loading...</span>
    </div>
    <div>Sending messages...</div>
  </div>
</div>

<style>
/* Modern WhatsApp UI Styling */
:root {
  --whatsapp-green: #25d366;
  --whatsapp-dark: #128c7e;
  --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --gradient-success: linear-gradient(135deg, #25d366 0%, #128c7e 100%);
  --gradient-warning: linear-gradient(135deg, #ffc107 0%, #ff8f00 100%);
  --shadow-modern: 0 8px 32px rgba(0, 0, 0, 0.1);
  --shadow-hover: 0 16px 48px rgba(0, 0, 0, 0.15);
  --border-radius: 16px;
}

/* Modern Card Styling */
.modern-card {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  border-radius: var(--border-radius) !important;
  overflow: hidden;
  background: linear-gradient(145deg, #ffffff 0%, #f8f9fa 100%);
}

.modern-card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-hover);
}

.modern-card .card-header {
  border-radius: var(--border-radius) var(--border-radius) 0 0 !important;
  position: relative;
  overflow: hidden;
}

.modern-card .card-header::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(45deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0.05) 100%);
  pointer-events: none;
}

/* Background Gradients */
.bg-gradient-primary {
  background: var(--gradient-primary) !important;
}

.bg-gradient-success {
  background: var(--gradient-success) !important;
}

.bg-gradient-warning {
  background: var(--gradient-warning) !important;
}

/* Modern Form Elements */
.modern-select, .modern-textarea {
  border-radius: 12px !important;
  border: 2px solid #e9ecef !important;
  padding: 12px 16px !important;
  transition: all 0.3s ease !important;
  background: #ffffff !important;
  font-size: 14px !important;
}

.modern-select:focus, .modern-textarea:focus {
  border-color: var(--whatsapp-green) !important;
  box-shadow: 0 0 0 0.2rem rgba(37, 211, 102, 0.25) !important;
  transform: translateY(-2px) !important;
}

.modern-textarea {
  resize: vertical !important;
  min-height: 120px !important;
}

/* Modern Buttons */
.modern-btn {
  border-radius: 12px !important;
  padding: 12px 24px !important;
  font-weight: 600 !important;
  text-transform: none !important;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1) !important;
  position: relative !important;
  overflow: hidden !important;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1) !important;
}

.modern-btn:hover {
  transform: translateY(-2px) !important;
  box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15) !important;
}

.modern-btn:active {
  transform: translateY(0) !important;
}

/* Modern Alert Styling */
.modern-alert {
  border-radius: 12px !important;
  border: 0 !important;
  padding: 20px !important;
  box-shadow: var(--shadow-modern) !important;
  backdrop-filter: blur(10px) !important;
}

/* Header Animations */
.bounce-animation {
  animation: bounce 2s infinite;
}

@keyframes bounce {
  0%, 20%, 50%, 80%, 100% {
    transform: translateY(0);
  }
  40% {
    transform: translateY(-10px);
  }
  60% {
    transform: translateY(-5px);
  }
}

.pulse-animation {
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.1);
  }
  100% {
    transform: scale(1);
  }
}

/* Mobile Responsive Enhancements */
@media (max-width: 768px) {
  .container-fluid {
    padding: 8px !important;
  }

  .modern-card {
    margin-bottom: 24px;
  }

  .modern-card .card-body {
    padding: 20px !important;
  }

  .modern-btn {
    padding: 14px 20px !important;
    font-size: 16px !important;
  }

  .modern-textarea {
    font-size: 16px !important; /* Prevent zoom on iOS */
  }

  .display-5 {
    font-size: 2rem !important;
  }

  .lead {
    font-size: 1rem !important;
  }
}

@media (max-width: 576px) {
  .row.mb-3 .col-6 {
    margin-bottom: 12px;
  }

  .modern-card .card-header h3 {
    font-size: 1.3rem;
  }

  .modern-card .card-header p {
    font-size: 0.85rem;
  }
}

.text-success {
  color: var(--whatsapp-green) !important;
}

/* Modern Modal Styling */
.modern-modal {
  border-radius: var(--border-radius) !important;
  border: 0 !important;
  box-shadow: var(--shadow-hover) !important;
  overflow: hidden !important;
}

.modern-modal .modal-header {
  border-radius: var(--border-radius) var(--border-radius) 0 0 !important;
  padding: 1.5rem !important;
  position: relative !important;
}

.modern-modal .modal-header::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(45deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0.05) 100%);
  pointer-events: none;
}

.modern-modal .modal-body {
  padding: 1.5rem !important;
  background: linear-gradient(145deg, #ffffff 0%, #f8f9fa 100%) !important;
}

.modern-modal .modal-footer {
  border-top: 0 !important;
  padding: 1.5rem !important;
  background: #f8f9fa !important;
}

/* Attractive Button Loader Styles */
.btn-with-loader {
  position: relative;
  transition: all 0.3s ease;
  overflow: hidden;
}

/* Check buttons loading states */
#check-unpaid-btn.loading {
  pointer-events: none;
  background: linear-gradient(-45deg, #fff3cd, #ffeaa7, #fff3cd, #ffeaa7);
  background-size: 400% 400%;
  animation: shimmer 1.5s ease-in-out infinite;
  border-color: #ffc107;
}

#check-total-customers-btn.loading {
  pointer-events: none;
  background: linear-gradient(-45deg, #d1e7dd, #a3d9b1, #d1e7dd, #a3d9b1);
  background-size: 400% 400%;
  animation: shimmer 1.5s ease-in-out infinite;
  border-color: #198754;
}

/* Send buttons loading states */
#send-individual-btn.loading {
  pointer-events: none;
  background: linear-gradient(-45deg, #0d6efd, #6ea8fe, #0d6efd, #6ea8fe);
  background-size: 400% 400%;
  animation: shimmer 1.5s ease-in-out infinite;
  border-color: #0d6efd;
}

#send-unpaid-btn.loading {
  pointer-events: none;
  background: linear-gradient(-45deg, #ffc107, #ffda6a, #ffc107, #ffda6a);
  background-size: 400% 400%;
  animation: shimmer 1.5s ease-in-out infinite;
  border-color: #ffc107;
}

#send-wishes-btn.loading {
  pointer-events: none;
  background: linear-gradient(-45deg, #198754, #51c878, #198754, #51c878);
  background-size: 400% 400%;
  animation: shimmer 1.5s ease-in-out infinite;
  border-color: #198754;
}

@keyframes shimmer {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%;
  }
}

.loading-dots {
  display: inline-block;
}

.loading-dots span {
  opacity: 0;
  animation: dot-flashing 1.4s infinite;
}

.loading-dots .dot2 {
  animation-delay: 0.2s;
}

.loading-dots .dot3 {
  animation-delay: 0.4s;
}

@keyframes dot-flashing {
  0%, 80%, 100% {
    opacity: 0;
  }
  40% {
    opacity: 1;
  }
}

/* Number Counter Animation */
.number-counter {
  display: inline-block;
  font-weight: bold;
  font-size: 1.2em;
  color: #ff6b35;
  animation: countUp 0.8s ease-out;
}

@keyframes countUp {
  from {
    transform: scale(0.5);
    opacity: 0;
  }
  50% {
    transform: scale(1.2);
  }
  to {
    transform: scale(1);
    opacity: 1;
  }
}

/* Modal Enhancements */
.modal-content {
  border: 0;
  border-radius: 15px;
  box-shadow: 0 15px 35px rgba(0,0,0,0.1);
}

.modal-header {
  border-radius: 15px 15px 0 0;
  border-bottom: 0;
  padding: 1.5rem;
}

.modal-body {
  padding: 1.5rem;
}

.list-group-item {
  border: 0;
  border-radius: 10px !important;
  margin-bottom: 0.5rem;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);
  transition: all 0.3s ease;
}

.list-group-item:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 15px rgba(0,0,0,0.12);
}

.customer-card {
  background: linear-gradient(135deg, #fff 0%, #f8f9fa 100%);
  border-left: 4px solid #ffc107;
}

.customer-card.high-amount {
  border-left-color: #dc3545;
  background: linear-gradient(135deg, #fff5f5 0%, #ffe6e6 100%);
}

.amount-badge {
  background: linear-gradient(135deg, #ffc107 0%, #ffb300 100%);
  color: #000;
  font-weight: 600;
  border-radius: 20px;
  padding: 0.5rem 1rem;
  box-shadow: 0 2px 8px rgba(255, 193, 7, 0.3);
}

.amount-badge.high {
  background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
  color: #fff;
  box-shadow: 0 2px 8px rgba(220, 53, 69, 0.3);
}

/* Unpaid Preview Styles */
#unpaid-preview .preview-item {
  background: #f8f9fa;
  border-radius: 8px;
  padding: 0.75rem;
  margin-bottom: 0.5rem;
  border-left: 3px solid #ffc107;
  transition: all 0.3s ease;
}

#unpaid-preview .preview-item:hover {
  background: #e9ecef;
  transform: translateX(5px);
}

/* Button pulse animation */
.btn-pulse {
  animation: pulse-highlight 1s ease-in-out infinite;
}

@keyframes pulse-highlight {
  0% {
    box-shadow: 0 0 0 0 rgba(255, 193, 7, 0.7);
  }
  70% {
    box-shadow: 0 0 0 10px rgba(255, 193, 7, 0);
  }
  100% {
    box-shadow: 0 0 0 0 rgba(255, 193, 7, 0);
  }
}

/* Select2 Customer Search Styling */
.select2-result-customer {
  display: flex;
  align-items: center;
  padding: 8px 12px;
}

.select2-result-customer__avatar {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: var(--whatsapp-green);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 12px;
  color: white;
  font-size: 14px;
}

.select2-result-customer__meta {
  flex-grow: 1;
}

.select2-result-customer__name {
  font-weight: 600;
  color: #333;
  margin-bottom: 2px;
}

.select2-result-customer__details {
  font-size: 12px;
  color: #6c757d;
}

/* Override Select2 selected item styling */
.select2-container--bootstrap-5 .select2-selection--single .select2-selection__rendered {
  background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%) !important;
  color: #333 !important;
  border-radius: 8px !important;
  padding: 8px 12px !important;
  border: 1px solid #ddd !important;
}

/* Selected customer in dropdown results */
.select2-container--bootstrap-5 .select2-results__option--highlighted {
  background: linear-gradient(135deg, #e3f2fd 0%, #f0f8ff 100%) !important;
  color: #333 !important;
}

.select2-container--bootstrap-5 .select2-results__option--selected {
  background: linear-gradient(135deg, #e8f5e8 0%, #f0fff0 100%) !important;
  color: #333 !important;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Initialize Select2 for customer search - wait for jQuery and Select2 to be available
  function initializeCustomerSearch() {
    if (typeof jQuery !== 'undefined' && jQuery.fn.select2) {
      $('#customer-search').select2({
        theme: 'bootstrap-5',
        placeholder: 'Search by name or phone number...',
        allowClear: true,
        minimumInputLength: 2,
        ajax: {
          url: '<%= search_customers_whatsapp_messages_path %>',
          dataType: 'json',
          delay: 250,
          data: function (params) {
            return {
              term: params.term
            };
          },
          processResults: function (data) {
            return {
              results: data.results
            };
          },
          cache: true
        },
        templateResult: function(customer) {
          if (customer.loading) return customer.text;

          var $container = $(
            '<div class="select2-result-customer">' +
              '<div class="select2-result-customer__avatar">' +
                '<i class="fas fa-user"></i>' +
              '</div>' +
              '<div class="select2-result-customer__meta">' +
                '<div class="select2-result-customer__name"></div>' +
                '<div class="select2-result-customer__details"></div>' +
              '</div>' +
            '</div>'
          );

          $container.find('.select2-result-customer__name').text(customer.name || customer.text);
          $container.find('.select2-result-customer__details').text(customer.phone || '');

          return $container;
        },
        templateSelection: function(customer) {
          return customer.name || customer.text || 'Select a customer...';
        }
      });
    } else {
      // Fallback for if Select2 isn't available - retry after a short delay
      setTimeout(initializeCustomerSearch, 100);
    }
  }

  // Initialize customer search
  initializeCustomerSearch();

  // Show message function
  function showMessage(message, type = 'success') {
    const container = document.getElementById('message-container');
    const alert = document.getElementById('message-alert');

    alert.className = `alert alert-${type}`;
    alert.innerHTML = `<i class="fas fa-${type === 'success' ? 'check-circle' : 'exclamation-triangle'} me-2"></i>${message}`;
    container.style.display = 'block';

    // Auto-hide after 5 seconds
    setTimeout(() => {
      container.style.display = 'none';
    }, 5000);
  }

  // Show/hide loading overlay
  function toggleLoading(show) {
    const overlay = document.getElementById('loading-overlay');
    if (show) {
      overlay.classList.remove('d-none');
    } else {
      overlay.classList.add('d-none');
    }
  }

  // 3. Check total customers functionality - Open modal with full customer list
  document.getElementById('check-total-customers-btn').addEventListener('click', function() {
    // Open modal immediately and show loading
    const modal = new bootstrap.Modal(document.getElementById('totalCustomersModal'));
    const modalCount = document.getElementById('modal-total-count');
    const modalCustomersList = document.getElementById('modal-total-customers-list');
    const modalLoading = document.getElementById('modal-total-loading');
    const modalNoCustomers = document.getElementById('modal-no-total-customers');

    // Show loading initially
    modalLoading.style.display = 'block';
    modalCustomersList.style.display = 'none';
    modalNoCustomers.style.display = 'none';
    modalCount.textContent = '...';

    modal.show();

    // Start button loader
    toggleButtonLoader('check-total-customers-btn', true);

    // Fetch total customers data
    fetch('<%= total_customers_data_whatsapp_messages_path %>')
    .then(response => response.json())
    .then(data => {
      toggleButtonLoader('check-total-customers-btn', false);

      // Hide loading
      modalLoading.style.display = 'none';

      if (data.count > 0) {
        // Animate count
        animateNumber(modalCount, 0, data.count);

        // Show customers list
        modalCustomersList.style.display = 'block';

        // Populate customers list
        modalCustomersList.innerHTML = data.customers.map((customer, index) => {
          return `
            <div class="list-group-item customer-card" style="animation-delay: ${index * 0.05}s">
              <div class="d-flex justify-content-between align-items-center">
                <div class="flex-grow-1">
                  <div class="d-flex align-items-center">
                    <div class="bg-success rounded-circle d-flex align-items-center justify-content-center me-3" style="width: 40px; height: 40px;">
                      <i class="fas fa-user text-white"></i>
                    </div>
                    <div>
                      <h6 class="mb-1 fw-bold">${customer.name}</h6>
                      <small class="text-muted">
                        <i class="fas fa-phone me-1"></i>${customer.phone_number}
                      </small>
                      ${customer.member_id ? `<div class="mt-1"><small class="badge bg-info">${customer.member_id}</small></div>` : ''}
                    </div>
                  </div>
                </div>
                <div class="text-end">
                  <span class="badge bg-success">
                    <i class="fas fa-check-circle me-1"></i>Active
                  </span>
                </div>
              </div>
            </div>
          `;
        }).join('');

        // Add entrance animation to customer cards
        setTimeout(() => {
          modalCustomersList.querySelectorAll('.customer-card').forEach((card, index) => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(20px)';
            setTimeout(() => {
              card.style.transition = 'all 0.5s ease';
              card.style.opacity = '1';
              card.style.transform = 'translateY(0)';
            }, index * 50);
          });
        }, 100);

        // Also update the info section in main form
        const infoDiv = document.getElementById('total-customers-info');
        const countSpan = document.getElementById('total-customers-count');
        countSpan.classList.add('number-counter');
        animateNumber(countSpan, 0, data.count);
        infoDiv.style.display = 'block';
      } else {
        // Show no customers message
        modalNoCustomers.style.display = 'block';
        modalCount.textContent = '0';
      }
    })
    .catch(error => {
      toggleButtonLoader('check-total-customers-btn', false);
      modal.hide();
      showMessage('Error fetching customer data.', 'danger');
      console.error('Error:', error);
    });
  });

  // 1. Individual Customer Message
  document.getElementById('individual-message-form').addEventListener('submit', function(e) {
    e.preventDefault();

    const customerId = $('#customer-search').val();
    const message = document.getElementById('individual-message').value.trim();

    if (!customerId) {
      showMessage('Please select a customer.', 'danger');
      return;
    }

    if (!message) {
      showMessage('Please enter a message.', 'danger');
      return;
    }

    toggleButtonLoader('send-individual-btn', true);

    fetch('<%= send_individual_message_whatsapp_messages_path %>', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
      },
      body: JSON.stringify({
        customer_id: customerId,
        message: message
      })
    })
    .then(response => response.json())
    .then(data => {
      toggleButtonLoader('send-individual-btn', false);
      if (data.success) {
        showMessage(data.message, 'success');
        // Reset form
        $('#customer-search').val(null).trigger('change');
        document.getElementById('individual-message').value = document.getElementById('individual-message').placeholder;
      } else {
        showMessage(data.error, 'danger');
      }
    })
    .catch(error => {
      toggleButtonLoader('send-individual-btn', false);
      showMessage('Error sending message. Please try again.', 'danger');
      console.error('Error:', error);
    });
  });

  // Store current unpaid customers data
  let currentUnpaidData = null;

  // Function to animate number counter
  function animateNumber(element, start, end, duration = 800) {
    const range = end - start;
    const increment = range / (duration / 16);
    let current = start;

    const timer = setInterval(() => {
      current += increment;
      if ((increment > 0 && current >= end) || (increment < 0 && current <= end)) {
        current = end;
        clearInterval(timer);
      }
      element.textContent = Math.floor(current);
    }, 16);
  }

  // Enhanced button loader function
  function toggleButtonLoader(buttonId, loading) {
    const btn = document.getElementById(buttonId);
    const btnContent = btn.querySelector('.btn-content');
    const btnLoader = btn.querySelector('.btn-loader');

    if (loading) {
      btn.classList.add('loading');
      btnContent.classList.add('d-none');
      btnLoader.classList.remove('d-none');
    } else {
      btn.classList.remove('loading');
      btnContent.classList.remove('d-none');
      btnLoader.classList.add('d-none');
    }
  }

  // 2. Check unpaid customers - Direct modal opening
  document.getElementById('check-unpaid-btn').addEventListener('click', function() {
    const month = document.getElementById('unpaid-month').value;
    const year = document.getElementById('unpaid-year').value;

    // Open modal immediately and show loading
    const modal = new bootstrap.Modal(document.getElementById('unpaidCustomersModal'));
    const modalMonthYear = document.getElementById('modal-month-year');
    const modalCount = document.getElementById('modal-unpaid-count');
    const modalTotalAmount = document.getElementById('modal-total-amount');
    const modalCustomersList = document.getElementById('modal-customers-list');
    const modalLoading = document.getElementById('modal-loading');
    const modalNoCustomers = document.getElementById('modal-no-customers');
    const modalSendBtn = document.getElementById('modal-send-reminder-btn');

    // Set modal header info
    const monthNames = ['', 'January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];
    modalMonthYear.textContent = `${monthNames[month]} ${year}`;

    // Show loading initially
    modalLoading.style.display = 'block';
    modalCustomersList.style.display = 'none';
    modalNoCustomers.style.display = 'none';
    modalCount.textContent = '...';
    modalTotalAmount.textContent = '...';

    modal.show();

    // Start button loader
    toggleButtonLoader('check-unpaid-btn', true);

    // Fetch data
    fetch(`<%= unpaid_customers_data_whatsapp_messages_path %>?month=${month}&year=${year}`)
    .then(response => response.json())
    .then(data => {
      toggleButtonLoader('check-unpaid-btn', false);
      currentUnpaidData = data; // Store for later use

      // Hide loading
      modalLoading.style.display = 'none';

      if (data.count > 0) {
        // Calculate total amount
        const totalAmount = data.customers.reduce((sum, customer) => sum + customer.unpaid_amount, 0);

        // Animate counts
        animateNumber(modalCount, 0, data.count);
        modalTotalAmount.textContent = `₹${totalAmount.toLocaleString('en-IN', {minimumFractionDigits: 2})}`;

        // Show customers list
        modalCustomersList.style.display = 'block';
        modalSendBtn.style.display = 'inline-block';

        // Populate customers list
        modalCustomersList.innerHTML = data.customers.map((customer, index) => {
          const isHighAmount = customer.unpaid_amount > 1000;
          return `
            <div class="list-group-item customer-card ${isHighAmount ? 'high-amount' : ''}" style="animation-delay: ${index * 0.1}s">
              <div class="d-flex justify-content-between align-items-center">
                <div class="flex-grow-1">
                  <div class="d-flex align-items-center mb-2">
                    <div class="bg-warning rounded-circle d-flex align-items-center justify-content-center me-3" style="width: 40px; height: 40px;">
                      <i class="fas fa-user text-dark"></i>
                    </div>
                    <div>
                      <h6 class="mb-1 fw-bold">${customer.name}</h6>
                      <small class="text-muted">
                        <i class="fas fa-phone me-1"></i>${customer.phone_number}
                      </small>
                    </div>
                  </div>
                  <div class="small text-muted">
                    <i class="fas fa-file-invoice me-1"></i>
                    ${customer.invoice_count} unpaid invoice${customer.invoice_count > 1 ? 's' : ''}
                  </div>
                </div>
                <div class="text-end">
                  <span class="amount-badge ${isHighAmount ? 'high' : ''}">
                    ₹${customer.unpaid_amount.toLocaleString('en-IN', {minimumFractionDigits: 2})}
                  </span>
                  <div class="mt-1">
                    <small class="text-muted">
                      ${isHighAmount ? '🔴 High Priority' : '⚠️ Pending'}
                    </small>
                  </div>
                </div>
              </div>
            </div>
          `;
        }).join('');

        // Add entrance animation to customer cards
        setTimeout(() => {
          modalCustomersList.querySelectorAll('.customer-card').forEach((card, index) => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(20px)';
            setTimeout(() => {
              card.style.transition = 'all 0.5s ease';
              card.style.opacity = '1';
              card.style.transform = 'translateY(0)';
            }, index * 100);
          });
        }, 100);

        // Enable send button in the main form
        document.getElementById('send-unpaid-btn').disabled = false;
      } else {
        // Show no customers message
        modalNoCustomers.style.display = 'block';
        modalSendBtn.style.display = 'none';
        modalCount.textContent = '0';
        modalTotalAmount.textContent = '₹0';

        // Disable send button in the main form
        document.getElementById('send-unpaid-btn').disabled = true;
      }
    })
    .catch(error => {
      toggleButtonLoader('check-unpaid-btn', false);
      modal.hide();
      showMessage('Error fetching unpaid customers.', 'danger');
      console.error('Error:', error);
    });
  });

  // 2. Send message to unpaid customers
  document.getElementById('unpaid-message-form').addEventListener('submit', function(e) {
    e.preventDefault();

    const month = document.getElementById('unpaid-month').value;
    const year = document.getElementById('unpaid-year').value;
    const message = document.getElementById('unpaid-message').value.trim();

    if (!message) {
      showMessage('Please enter a message.', 'danger');
      return;
    }

    if (confirm('Are you sure you want to send this message to all unpaid customers?')) {
      toggleButtonLoader('send-unpaid-btn', true);

      fetch('<%= send_unpaid_customers_message_whatsapp_messages_path %>', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
        },
        body: JSON.stringify({
          month: month,
          year: year,
          message: message
        })
      })
      .then(response => response.json())
      .then(data => {
        toggleButtonLoader('send-unpaid-btn', false);
        if (data.success) {
          showMessage(data.message, 'success');
        } else {
          showMessage(data.error, 'danger');
        }
      })
      .catch(error => {
        toggleButtonLoader('send-unpaid-btn', false);
        showMessage('Error sending messages. Please try again.', 'danger');
        console.error('Error:', error);
      });
    }
  });

  // 3. Send wishes to all customers
  document.getElementById('wishes-message-form').addEventListener('submit', function(e) {
    e.preventDefault();

    const message = document.getElementById('wishes-message').value.trim();

    if (!message) {
      showMessage('Please enter a message.', 'danger');
      return;
    }

    if (confirm('Are you sure you want to send this message to ALL customers? This action cannot be undone.')) {
      toggleButtonLoader('send-wishes-btn', true);

      fetch('<%= send_wishes_to_all_whatsapp_messages_path %>', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
        },
        body: JSON.stringify({
          message: message
        })
      })
      .then(response => response.json())
      .then(data => {
        toggleButtonLoader('send-wishes-btn', false);
        if (data.success) {
          showMessage(data.message, 'success');
        } else {
          showMessage(data.error, 'danger');
        }
      })
      .catch(error => {
        toggleButtonLoader('send-wishes-btn', false);
        showMessage('Error sending messages. Please try again.', 'danger');
        console.error('Error:', error);
      });
    }
  });

  // Month/year change handlers - no auto-loading
  document.getElementById('unpaid-month').addEventListener('change', function() {
    // Just update the button text to reflect the change
    // User needs to click the button to actually load data
  });

  document.getElementById('unpaid-year').addEventListener('change', function() {
    // Just update the button text to reflect the change
    // User needs to click the button to actually load data
  });

  // Modal send reminder functionality
  document.getElementById('modal-send-reminder-btn').addEventListener('click', function() {
    const modal = bootstrap.Modal.getInstance(document.getElementById('unpaidCustomersModal'));
    modal.hide();

    // Scroll to unpaid customers section and trigger send
    document.querySelector('#unpaid-message-form').scrollIntoView({ behavior: 'smooth' });

    // Highlight the send button
    const sendBtn = document.getElementById('send-unpaid-btn');
    sendBtn.classList.add('btn-pulse');
    setTimeout(() => sendBtn.classList.remove('btn-pulse'), 2000);
  });


  // No auto-loading on page load - user must click buttons to load data
});
</script>